# Browse 锚点「关于万年」识别失败修复报告

## 问题现象
日志显示：
```
E:站内未找到目标页（锚点：「关于万年」），已在当前页滚动。候选链接：关于万年 | 关于万年 | 关于万年 | 万年
```

用户目标站 `https://www.manincorp.cn/index.html` 导航栏最后一个确实是「关于万年」，但 BROWSE 步骤软失败。

## 根因分析
1. 锚点**文本已经识别到了**：`matchContactLink('关于万年', 'about.html', '关于万年')` 返回 `true`（子串匹配）。
2. 失败点在 `_findContactLink` 的 URL 解析逻辑：原代码只处理两种 href：
   - `/` 开头的绝对路径（拼接 `base.origin`）
   - 完整的 `http://` / `https://` URL
3. 而 manincorp.cn 的 HTML 是：
   ```html
   <li><a href="about.html">关于万年</a></li>
   ```
   `href="about.html"` 是**相对路径**，原代码没有处理，所以 `matchContactLink` 命中后仍返回 `null`。
4. `_collectLinkDiag` 只收集文本，不显示 href，所以日志只能看到「候选链接：关于万年」，看不到 href 是相对路径 `about.html`。

## 修复内容
### `core/taskEngine.js`
- `_findContactLink`：统一使用 `new URL(href, base.href)` 解析 href，支持：
  - `/about.html` → `https://www.manincorp.cn/about.html`
  - `about.html` → `https://www.manincorp.cn/about.html`
  - `http://...` / `https://...` → 原样保留
- 过滤伪链接：`#`、`javascript:`、`mailto:`、`tel:`、`data:`（点了不会跳转真实页面）
- `_collectLinkDiag`：诊断信息同时输出文本和解析后的 URL，便于排查。

### 新增测试
`test/browseRelativeLink.test.js`（4 用例）：
1. 相对路径 `about.html` → 正确解析为绝对 URL
2. 绝对路径 `/about.html` → 正确解析
3. 完整 `http://` URL → 正确返回
4. `#` / `javascript:` 伪链接 → 忽略

## 验证结果
```bash
node --test test/*.test.js
# tests 203
# pass 202
# fail 0
# skipped 1
```

## 生效前提
当前运行中的 autoclaw 服务（PID 33948）是旧代码启动的，需在交互桌面会话里重启一次：
```bat
cd C:\Users\kan\WorkBuddy\autoclaw
set AUTOCLAW_DB_TYPE=sqlite
set AUTOCLAW_TOKEN=autoclaw-dev
set PORT=7788
node app.js
```

重启后重新提交任务，BROWSE 步骤应能正常点击「关于万年」并进入 `about.html`。

## 重启 + 验证（16:15）

### 服务重启
- `taskkill /PID 33948` 终止旧进程（旧代码），端口释放。
- 用正确环境变量后台启动新代码：`AUTOCLAW_DB_TYPE=sqlite AUTOCLAW_TOKEN=autoclaw-dev PORT=7788 node app.js`
- 新进程 PID **9532**，会话 `Console 1`（桌面交互会话，本机 Chrome 可见窗口可弹出）。
- 新路由生效证据：`GET /api/client/list` 返回 `401`（未授权）而非 `404`（旧代码无此路由），说明新代码已加载。
- `/api/status` 正常：`{"service":"autoclaw","version":"0.1.0","status":null}`

### 真实页面验证（scripts/verify-browse-anchor.js）
直接用 Playwright 打开 `https://www.manincorp.cn/index.html`，用修复后的解析逻辑（`matchContactLink` + `new URL(href, base.href)`）匹配锚点「关于万年」：

```
ANCHOR=关于万年
HITS=[
  { "text": "关于万年", "resolved": "https://www.manincorp.cn/about.html" },
  { "text": "关于万年", "resolved": "https://www.manincorp.cn/about.html" },
  { "text": "关于万年", "resolved": "https://www.manincorp.cn/about.html" },
  { "text": "万年",     "resolved": "https://www.manincorp.cn/about.html" }
]
```

4 个候选（导航「关于万年」×3 + 移动端「万年」）全部正确解析为绝对 URL `https://www.manincorp.cn/about.html`。**修复前这些链接的 `href="about.html"` 是相对路径，会被忽略返回 null。**

### 端到端任务重测说明
通过 API 提交重测任务（`keywords=["万年移民"]`），但本次百度实时排名中 `manincorp.cn` 未进入「万年移民」搜索结果前 10，`locate` 步骤失败（非代码问题，是 SEO 真实效果/排名波动）。任务在 `locate` 即熔断，未走到 `browse` 步骤，因此**端到端 browse 需待目标站进入搜索前 10 才能观察到**。

结论：代码层修复 + 真实页面解析已 100% 验证；端到端需目标关键词能搜进前 10 才能跑通（建议在前端提交一个当前能进前 10 的关键词做最终确认）。
