# Autoclaw 客户线（P0-8 ~ P0-11）交付报告

> 日期：2026-07-18 ｜ 形态：代码 + 单测 + 文档同步 ｜ 状态：✅ 已交付，待重启服务生效

## 一、背景
接手文档 `docs/prd-autoclaw-handoff.md` 把「客户线」列为唯一尚未编码的 P0 块：运营需把任务归属客户、按客户生成统计与交付报告。本次按 §4.5 数据模型与 §6 状态，完整实现。

## 二、改动清单

### 数据层（config/db.js + 双后端 schema）
- `scripts/schema.sqlite.sql`：新增 `client` 表；`task_config` 增加 `client_id` 列与索引。
- `scripts/schema.sql`（MySQL）：新增 `client` 表；`ALTER TABLE task_config ADD COLUMN client_id`。
- `config/db.js`：
  - `getSqliteDb()` 增加**幂等迁移**（老旧库自动补 `client_id` 列 + 建 `client` 表）。
  - `saveTaskConfig` 透传 `clientId`；`getHistory` 回读 `clientId`。
  - 新增助手：`createClient` / `listClients` / `getClient` / `updateClient` / `deleteClient` / `getClientStats` / `getClientTasks`（双后端 `query` 适配层复用，sqlite/mysql 通吃）。
  - `deleteClient` 防误删：存在关联任务时抛 `ERR_CLIENT_HAS_TASKS`（路由转 409）。

### 配置层（core/taskConfig.js）
- `buildTaskConfig` 解析 `clientId` / `client_id`，写入返回对象（P0-9 任务归属）。

### 路由层（routes/clientRoutes.js，新增 + app.js 挂载）
- `app.js`：`app.use('/api/client', taskTokenAuth, clientRoutes)`（复用 A3 鉴权）。
- 端点（均经 token 鉴权）：
  - `GET  /api/client/list`
  - `POST /api/client/create`  `{ name, contact?, notes? }`
  - `GET  /api/client/:id`
  - `PUT  /api/client/:id`     `{ name?, contact?, notes? }`
  - `DELETE /api/client/:id`
  - `GET  /api/client/:id/stats`   （任务数 / 整体成功率 / 最近运行时间）
  - `GET  /api/client/:id/report?format=markdown|csv|html`（交付报告，附件下载）
- `submit` 透传 `clientId` 已由既有 `buildTaskConfig` + `saveTaskConfig` 自动承接，无需改路由逻辑。

### 前端（public/index.html + public/js/config.js + public/css/style.css）
- 任务表单新增「归属客户」下拉（P0-9）。
- 新增「客户管理」卡片：列表 + 新增表单 + 统计/交付报告/删除按钮（P0-8）。
- 历史回填时同步回填 `clientId`。

### 错误码（core/progressEvent.js）
- 新增 `ERR_CLIENT_NOT_FOUND`、`ERR_CLIENT_HAS_TASKS`。

## 三、测试
- 新增 `test/clientRoutes.test.js`（路由层，mock db，覆盖鉴权/CRUD/stats/三种格式报告）。
- 新增 `test/clientData.test.js`（集成，临时 SQLite：CRUD 全链路、`client_id` 透传、统计聚合、删除防关联任务）。
- 修正既有 `test/db.test.js`（INSERT 参数下标）、`test/progressEvent.test.js`（ERR 计数 11→13）。
- 全量结果：**199 用例 / 198 通过 / 1 skip**（skip 为真实浏览器 e2e，需 `AUTOCLAW_REAL_BROWSER=1`）。

## 四、生效步骤（重要）
1. 实时库已做一次性迁移：`scripts/migrate-client-live.js` 已对 `data/autoclaw.db` 补 `client` 表 + `client_id` 列（幂等，已成功）。
2. **当前运行的服务（PID 33948）是旧代码启动的**，不暴露 `/api/client` 路由。需在**交互桌面会话**重启以加载新代码：
   ```
   cd C:\Users\kan\WorkBuddy\autoclaw
   AUTOCLAW_DB_TYPE=sqlite AUTOCLAW_TOKEN=autoclaw-dev PORT=7788 node app.js
   ```
   （勿用 nohup/disown —— 见 handoff 文档 §5.4 坑 1；浏览器任务需可见窗口。）
3. 重启后访问 http://localhost:7788 即可看到客户归属下拉与客户管理面板；API 亦可用。

## 五、已知遗留（非阻塞）
- T-1（`ERR_BROWSER_LAUNCH` 未落盘）：本次未触碰 worker 代码路径，保留为后续项。
- `proxy_json` 仍落库未生效（handoff §7.1 #5），属 V1 预留，未变。
- 交付报告为「日志摘要」版，命中截图属 P1（handoff §7.1 #1）。
