# Autoclaw 开发进度巡检（2026-07-18 15:25，GMT+8）

> 工作空间：`C:\Users\kan\WorkBuddy\autoclaw`（非 git 仓库）｜ 巡检人：WorkBuddy

## 总体结论
项目已完成本地化搭建、核心引擎可跑通，且**上次 zip 里暴露的「locate 双匹配过严 → 100% 熔断」bug 已修复并交付（QA 180/181 通过）**。当前主要缺口：客户线（CRM/交付报告）未实现、谷歌适配器待 VPN 实测。

## 1. 环境与运行状态
| 项 | 状态 |
|----|------|
| 工作空间 | `C:\Users\kan\WorkBuddy\autoclaw`，**非 git 仓库**（无提交历史，靠文件 mtime + 测试 + DB 判断进度） |
| 依赖 | `npm install` 完成（126 包；`PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`；better-sqlite3 原生模块已编译） |
| 服务 | ✅ 运行中，PID **33948**，监听 `0.0.0.0:7788`，version **0.1.0**，uptime ~28min，`activeTaskId=null`（当前无活跃任务） |
| 数据库 | SQLite（`data/autoclaw.db`）：`task_config` 1 行、`task_run_log` 16 行 |
| 配置 | `.env` 已生成（`AUTOCLAW_DB_TYPE=sqlite`）；日志目录仅 `.gitkeep`（运行日志走 stdout/socket） |

## 2. 代码进展（对比 handoff zip）
- 百度搜索 E2E：✅ 跑通（search → 进站 → 停留/滚动）
- **locate 双匹配修复 ✅ 已交付**：抽出 `PlatformAdapter.matchTarget(items,target,opts)` 纯函数，重构 `baiduAdapter.locateTarget`（`mu`/`data-url` 优先 + domain-only 兜底 + 可诊断 Error）。改动 `platformAdapter.js` / `baiduAdapter.js` + 新增 3 个测试（`matchTarget.test.js` / `targetMatch.test.js` / `baiduAdapter.qa.test.js`）。
- 单元测试：**基线 155 → 现 181 用例 / 180 通过 / 1 skip**（新增 26 例，集中在 target 匹配），全绿。
- 最近改动文件（14:44–14:53）：`core/adapters/baiduAdapter.js`、`core/adapters/platformAdapter.js`、3 个匹配测试。

## 3. 修复后真实任务实测（提交于 15:14，即 locate 修复之后）
- 任务：百度，关键词 `[万年移民, 移民公司, 移民中介]`，目标 `manincorp.cn`
- 运行记录：`search✅×2`、`locate✅×1 + ❌×1`、`enter✅`、`stay✅`、`browse❌×1`、`close✅`；终态 **failed**
- 解读：相比 zip 里 100% locate 失败，**修复后至少 1 个关键词成功定位并进站**——匹配逻辑改善已见效；整体仍 `failed` 大概率是部分关键词未进百度前 10 + browse 软失败触发熔断（真实 SEO 效果问题，非崩溃）。

## 4. 未实现 / 待办
- ❌ **客户线**（client CRUD / 交付报告）：无 `client` 表、`task_config` 无 `client_id` 列——仅数据模型就绪。
- 🟡 **谷歌适配器**：代码就位，需 VPN 环境实测（本地不可测）。
- 🟡 **代理注入**：`proxy_json` 已落库但未实际生效。
- ⚠️ **文档略滞后**：handoff §9 已标注，残留风险（真实百度 `mu`/`data-url` 稳定性、HEAD 跳转可靠性、baidu 抛错 vs google 返回 null 契约不一致）。

## 5. ⚠️ 启动坑（务必记牢）
- 项目**无 dotenv**：`.env` 不会被自动加载，`app.js`/`config/db.js` 只读 `process.env`。
- 纯 `node app.js` → `AUTOCLAW_DB_TYPE` 默认 **mysql** → 连 `localhost:3306` 失败（本机无 MySQL）。
- **正确启动（sqlite 模式，必须在交互桌面会话）**：
  ```bat
  cd C:\Users\kan\WorkBuddy\autoclaw
  AUTOCLAW_DB_TYPE=sqlite AUTOCLAW_TOKEN=autoclaw-dev PORT=7788 node app.js
  ```
- 控制台 UI：http://localhost:7788 ，令牌 `autoclaw-dev`（页面输入框留空即默认）。

## 6. 建议下一步
1. 用持久化 profile 手动登录百度降验证码：`AUTOCLAW_CHROME_USER_DATA=<固定目录>`（单任务串行，避免 ProcessSingleton 锁）。
2. 多关键词回归，验证 locate 修复在不同排名下的稳定性。
3. 按业务优先级决定是否启动客户线 / 谷歌适配器。
